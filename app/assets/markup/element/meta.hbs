<meta
    {{#if this.name}}
    name="{{this.name}}"
    {{/if}}
    {{#if this.content}}
    content="{{this.content}}"
    {{/if}}
    {{#if this.property}}
    property="{{this.property}}"
    {{/if}}
>